using UnityEngine;
using System.Collections;

public class StreetWalkers : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {

        //this.rigidbody.velocity = new Vector3(0, 0, -50);
	
	}
}
